{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && npx --yes prisma@5.20.0 generate"
  },
  "deploy": {
    "startCommand": "npx --yes prisma@5.20.0 db push --accept-data-loss && node src/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

